datasets:
  hr_employees:
    description: Employee master data including payroll
    owner: HR
    contact: hr-data@company.com
    datatype: Table
    location:
      raw: data/raw/hr_employees.csv
      processed: "REPLACE ME WHEN PROCESSED"
    classification: CONFIDENTIAL
    contains_pii: true
    pii_fields:
      - name
      - email
      - ssn
      - salary
    allowed_uses:
      - Payroll
      - HR Analytics
    llm_policy:
      allowed: false
      reason: "Contains PII"
    retention:
      raw_days: 7
      processed_days: 365